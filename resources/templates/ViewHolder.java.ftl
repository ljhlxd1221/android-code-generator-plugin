import android.view.View;
${IMPORTS}

/**
 *
 * Created by Android Code Generator.
 *
 */
public class ${CLASS_NAME}ViewHolder extends ViewHolder ${INTERFACES} {

    public static final int LAYOUT_ID = R.layout.${RESOURCE_NAME};

    ${FIELDS}

    public ${CLASS_NAME}ViewHolder(View view) {
        super(view);

        ${ASSIGNMENTS}
    }

    ${METHODS}
}
